anki:
  apt:
    app: anki

atom:
  apt:
    repo: ppa:webupd8team/atom
    app: atom
  plugin:
    01: terminal-plus

basicos:
  apt:
    app: gcc libavcodec-extra make p7zip pinta synaptic gnome-software-plugin-flatpak chrome-gnome-shell audacious

calibre:
  apt:
    app: calibre

dolphin:
  apt:
    repo: ppa:dolphin-emu/ppa
    app: dolphin-emu

flatpak:
  apt:
    app: flatpak
  flatremote:
    flathub: flathub https://flathub.org/repo/flathub.flatpakrepo

neovim:
  apt:
    app: neovim

gnomegames:
  apt:
    app: gnome-games-app

powerline:
  apt:
    app: fonts-powerline

retroarch:
  apt:
    repo: ppa:libretro/stable
    key:
      keyserver: keyserver.ubuntu.com
      recvkeys: 18DAAE7FECA3745F
    app: retroarch retroarch-* libretro-*

telegram:
  apt:
    repo: ppa:atareao/telegram
    app: telegram

tilix:
  apt:
    app: tilix
  vte: sudo ln -s /etc/profile.d/vte-2.91.sh /etc/profile.d/vte.sh
  conf:
    dilocal: confs/shells/tilix/tilix.dconf.dump
    dump: dconf dump /com/gexperts/Tilix/ >
    load: dconf load /com/gexperts/Tilix/ <

tmux:
  apt:
    app: tmux

transmission:
  apt:
    app: transgui transmission-daemon
    remove: transmission-gtk

vim:
  apt:
    app: vim

visualstudiocode:
  snap:
    app: code
    parametro: --classic

zsh:
  apt:
    app: zsh
  ohmyzsh:
    git: ADD LINK
    plugins:
      autosuggestions: git clone git://github.com/zsh-users/zsh-autosuggestions $ZSH_CUSTOM/plugins/zsh-autosuggestions
